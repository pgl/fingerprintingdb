#!/bin/bash

set -e

source 'bin/common.sh'

for warcfile in sourcedb/**/*.warc
do
    site=$(getsite "$warcfile")

    echo "checking $site"

    output=$(bin/testwarc "$warcfile")

    [[ "$output" ]] && echo "$output" >> "detectiondb/$site.json"
done
